# Copiar este formato en Streamlit Cloud > App > Settings > Secrets.
# No subir el archivo real secrets.toml al repositorio.

DATABASE_URL = "postgresql://usuario:password@host.neon.tech/dbname?sslmode=require"
